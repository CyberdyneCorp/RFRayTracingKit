# core-io-formats

First-class geospatial import and export at the C++ core level: a scene georeference,
GeoJSON / CityJSON / OSM (Overpass JSON) / MSI antenna import, CZML and 3D-Tiles export,
and flag-gated GeoTIFF terrain + heatmap (GDAL) and Parquet result (Arrow) IO.

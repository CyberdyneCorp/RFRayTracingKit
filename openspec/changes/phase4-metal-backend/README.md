# phase4-metal-backend

Metal GPU ray-tracing backend: MTLAccelerationStructure build + runtime-compiled metal_raytracing intersect/occlusion kernels behind the existing IBackend interface, an additive batched-query API on IBackend, backend selection with CPU fallback, and CPU-vs-Metal parity tests.
